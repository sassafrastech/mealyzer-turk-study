<div class="matches">
  <div class="row">
    <div class="col-md-10">
      <h3>Instructions</h3>
      <p>In each photo below, a box containing  one food item will be highlighted. Next to it, you will see a description of this item and its ingredients identified by you or other Turkers. Your job is to match each of these ingredients to the four food groups. These food groups include carbohydrates, fats, protein, and fiber. You need to map each ingredient to one food group.</p>
    </div>
  </div>
  <div class="row">
  <%= render partial: "shared/flash_messages", flash: flash %>
    <div class="col-md-5">
      <%= image_tag @meal.photo %>
      <% #DRAW Rectangle %>
    </div>
    <div class="col-md-6">
      <h3><%= @meal.food_options %></h3>

      <%= simple_form_for @match_answer do |f| %>
        <h3><%= @match_answer.sample_component.name %></h3>
        <%= f.hidden_field :meal_id, :value => @meal.id %>
        <%= f.hidden_field :user_id, :value => 12 %>
        <%= f.hidden_field :sample_component, :value => @match_answer.sample_component.to_json %>
        <table>
          <tr><td>What is it made of?</td><td colspan="3">What food group does it belong to?</td></tr>
          <tr class="groups">
            <td></td>
            <% Meal::GROUPS.each do |h| %>
              <td><%= h %></td>
            <% end %>
          </tr>
          <% @match_answer.sample_component.items.each do |food_item| %>
            <tr>
              <td><label><%= food_item %></label></td>
              <% Meal::GROUPS.each do |nutrition| %>
                <td class="cb">
                 <%= check_box_tag "match_answer[food_groups][#{@match_answer.sample_component.name}][#{food_item}][]", nutrition, @match_answer.cb_checked?(food_item, nutrition) %>
              <% end %>
            </tr>
          <% end %>
        </table>
        <br/>
        <%= f.submit :submit, :class => "btn btn-primary" %>
      <% end %>
    </div>
  </div>
</div>

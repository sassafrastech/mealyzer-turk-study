<h3>Thank you!</h3>
<%= turkee_form_for(@user, params, {:assignment_id => @user.assignmentId}) do |f| %>
  <h4>Click submit to finish</h4>
  <input id="assignmentId" name="assignmentId" type="hidden" value="<%= @user.assignmentId %>" />

  <%= f.submit :submit, :class => "btn btn-primary" %>
<% end %>
